file(GLOB SDLMM_SRC "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp" "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp")
if(SDLMM_SRC)
  if(ASC_USE_SDL1 OR ASC_USE_SDL12COMPAT)
    add_library(SDLmm STATIC ${SDLMM_SRC})
    target_include_directories(SDLmm PRIVATE ${CMAKE_SOURCE_DIR}/source ${CMAKE_CURRENT_SOURCE_DIR}/src)
    if(DEFINED ASC_SDL_INCLUDE_DIR)
      target_include_directories(SDLmm PRIVATE ${ASC_SDL_INCLUDE_DIR})
    endif()
  else()
    message(WARNING "SDLmm requires SDL1 API; skipping SDLmm build (install libsdl1.2-dev or sdl12-compat)")
  endif()
endif()
